remote_theme: pages-themes/minimal@v0.2.0
plugins:
  - jekyll-remote-theme # add this line to the plugins list if you already have one

title: Bearleargais
description: Translation of Nualeargais.

# Define a collection for chapters
collections:
  chapters:
    output: true
    permalink: /:path/
    title: "Chapters"

# Override navigation to include collection items
nav:
  - title: "Home"
    url: "/"
